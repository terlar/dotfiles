[Unit]
Description=Dropbox service

After=network.target
Requires=network.target

[Service]
ExecStart=/home/terje/.dropbox-dist/dropbox
ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=default.target
