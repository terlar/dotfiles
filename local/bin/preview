#!/usr/bin/env bash
initialize() {
	local file="$*"
	case "$file" in
		*.rst)
			rst2html $file | browser
			;;
		*.md)
			pandoc -s $file | browser
			;;
		*)
			echo "File $file is of unknown type"
			;;
	esac
}

initialize "$@"
