#!/usr/bin/env bash
if [ -z "$TMUX" ]; then
	mux monitoring monitor
else
	tmux new-window -n processes 'top'
	tmux new-window -n systemd 'journalctl -f'
	tmux new-window -n kernel 'dmesg -w --human'
fi
