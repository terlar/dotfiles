#!/usr/bin/env bash
if ! ssh -q docker-skype true; then
	echo -n 'Starting Skype Server'
	systemctl --user start skype

	until ssh -q docker-skype true; do
		echo -n '.'
		sleep 0.1
	done

	echo
fi

ssh docker-skype skype-pulseaudio &
