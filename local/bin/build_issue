#!/usr/bin/env bash
main() {
	local variant=${1:-light}
	local script_path="$HOME/.vconsole_$variant"

	[[ -f $script_path ]] \
		&& source $script_path
	echo "Arch Linux \\r (\\l)"
	echo
}

main "$@"
