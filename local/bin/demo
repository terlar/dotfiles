#!/usr/bin/env bash
port="${1-80}"
subdomain="${2-$USER-${PWD##*/}}"
username="$3"
password="$4"

if [ -n "$username"  ] && [ -n "$password" ]; then
	httpauth="-httpauth=${username}:${password}"
fi

ngrok -subdomain="$subdomain""$httpauth" "$port"
