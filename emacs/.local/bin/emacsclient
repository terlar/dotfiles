#!/usr/bin/env bash
main() {
	fd=0 # stdin

	if [[ -n "$DISPLAY" ]] && [[ -t "${fd}" || -p /dev/stdin ]]
	then
		if command -v xdotool >/dev/null 2>&1
		then
			xdotool key 'Super+E'
		fi
	fi

	/usr/bin/emacsclient "$@"
}

main "$@"
