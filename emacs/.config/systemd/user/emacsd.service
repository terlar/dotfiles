[Unit]
Description=Emacs Daemon
Documentation=man:emacs(1)

[Service]
Type=forking
Environment=LANG=zh_CN.UTF-8
ExecStart=/usr/bin/remacs --daemon
ExecStop=/usr/bin/remacsclient -c --eval "(kill-emacs)"

[Install]
WantedBy=default.target
