# Paths
HOME            DEFAULT=/home/@{PAM_USER} OVERRIDE=${HOME}
PATH            DEFAULT=${HOME}/.local/bin:${HOME}/.cabal/bin:/usr/local/sbin:/usr/local/bin:/usr/bin:/usr/bin/core_perl

# Display
REMOTEHOST      DEFAULT=localhost OVERRIDE=@{PAM_RHOST}
DISPLAY         DEFAULT=:0        OVERRIDE=${DISPLAY}

# XDG
XDG_CACHE_HOME  DEFAULT=${HOME}/.local/cache
XDG_CONFIG_HOME DEFAULT=${HOME}/.config
XDG_DATA_HOME   DEFAULT=${HOME}/.local/share

# Tools
VISUAL          DEFAULT=vim
EDITOR          DEFAULT=vim
BROWSER         DEFAULT=xdg-open
