#!/usr/bin/env bash
set -euo pipefail

main() {
	local playlist line url

	playlist="${1}"
	line="${2:-1}"
	url=$(sed -E '/^(#|\s*$)/d;s/\s#\s.*//' "${playlist}" \
		      | sed "${line}q;d")

	mpv "${url}"
}

main "$@"
