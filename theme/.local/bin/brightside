#!/usr/bin/env bash
[[ "${TRACE}" ]] && set -x
set -eu
set -o pipefail

has_display() {
	[[ -n "$DISPLAY" ]]
}

main() {
	if has_display; then
		ln -sf "$HOME/.config/termite/config_light" "$HOME/.config/termite/config"
		killall -USR1 termite
	else
		"$HOME/.vconsole_light"
		clear
	fi
}

main
